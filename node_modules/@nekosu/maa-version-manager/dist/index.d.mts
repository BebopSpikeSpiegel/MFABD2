import pacote from "pacote";

//#region pkgs/maa-version-manager/src/index.d.ts
type NpmRegistryType = keyof typeof MaaVersionManager.registries;
declare class MaaVersionManager {
  static readonly registries: {
    readonly npm: "https://registry.npmjs.org";
    readonly cnpm: "https://registry.npmmirror.com";
  };
  static isValidRegistryType(key: unknown): key is NpmRegistryType;
  root: string;
  registry: string;
  get downloadPath(): string;
  get installPath(): string;
  constructor(root: string);
  init(): Promise<void>;
  lock(): Promise<(() => Promise<void>) | null>;
  versionFolder(version: string): string;
  moduleFolder(version: string): string;
  fetchAllLocalVersions(): Promise<string[]>;
  fetchAllVersions(minimumVersion: string): Promise<[string, pacote.AbbreviatedManifest][]>;
  fetchLatest(): Promise<(pacote.AbbreviatedManifest & pacote.ManifestResult) | null>;
  prepare(version: string, progress: (msg: 'prepare-folder' | 'download-scripts' | 'download-binary' | 'move-folders' | 'finish') => void): Promise<boolean>;
  cleanUnused(skipVersions?: string[]): Promise<void>;
}
//#endregion
export { MaaVersionManager, NpmRegistryType };
//# sourceMappingURL=index.d.mts.map